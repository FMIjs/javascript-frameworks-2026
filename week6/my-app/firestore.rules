rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Courses are readable by anyone; writes require a signed-in user (seed runs after login).
    match /courses/{courseId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
